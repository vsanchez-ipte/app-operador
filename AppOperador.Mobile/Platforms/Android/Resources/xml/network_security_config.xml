<?xml version="1.0" encoding="utf-8"?>
<!--
	Permite HTTP sin cifrar SOLO contra el API de desarrollo.

	Desde Android 9 el tráfico en claro está bloqueado por omisión, así que sin esto la
	llamada a http://10.0.2.2:5231 falla con "Cleartext HTTP traffic not permitted" antes
	de salir del dispositivo.

	Se listan destinos concretos en vez de abrir `cleartextTrafficPermitted` global: así el
	permiso no acompaña a la app cuando apunte al API real, que irá por HTTPS.

	  · 10.0.2.2  → el equipo anfitrión visto desde el emulador de Android
	  · localhost → el propio dispositivo, útil con `adb reverse`
-->
<network-security-config>
	<domain-config cleartextTrafficPermitted="true">
		<domain includeSubdomains="false">10.0.2.2</domain>
		<domain includeSubdomains="false">localhost</domain>
		<domain includeSubdomains="false">127.0.0.1</domain>
	</domain-config>
</network-security-config>
