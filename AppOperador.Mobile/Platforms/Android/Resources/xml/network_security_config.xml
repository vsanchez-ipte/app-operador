<?xml version="1.0" encoding="utf-8"?>
<!--
	Permite HTTP sin cifrar SOLO contra los servidores de desarrollo.

	Desde Android 9 el tráfico en claro está bloqueado por omisión, así que sin esto la
	llamada al API falla con "Cleartext HTTP traffic not permitted" antes de salir del
	dispositivo.

	Se listan destinos concretos en vez de abrir `cleartextTrafficPermitted` global: así el
	permiso no acompaña a la app cuando apunte al API de producción, que irá por HTTPS.

	  · 10.0.2.2        → el equipo anfitrión visto desde el emulador de Android
	  · localhost       → el propio dispositivo, útil con `adb reverse`
	  · 192.168.100.215 → servidor de desarrollo de IPTE, que no publica el 443
	  · 192.168.100.230 → servidor de QA de IPTE; comprobado que tampoco publica el 443

	Este archivo se retira cuando todos los ambientes hablen HTTPS (criterio pendiente de
	JTT-1378). Si el certificado del servidor lo emite una autoridad interna de IPTE, hay
	que declararla aquí antes de retirar el resto.
-->
<network-security-config>
	<domain-config cleartextTrafficPermitted="true">
		<domain includeSubdomains="false">10.0.2.2</domain>
		<domain includeSubdomains="false">localhost</domain>
		<domain includeSubdomains="false">127.0.0.1</domain>
		<domain includeSubdomains="false">192.168.100.215</domain>
		<domain includeSubdomains="false">192.168.100.230</domain>
	</domain-config>
</network-security-config>
